# Purpose: production‑like, no mocks, but cost‑efficient defaults.
# Use real embeddings with conservative rate‑limits; keep optional providers off.

# ========= Embeddings (Primary: Gemini) =========
# Real vectors, conservative rate-limits (no DRYRUN).
GEMINI_API_KEY=<YOUR_API_KEY>
GEMINI_EMB_MODEL=models/text-embedding-004
GEMINI_EMB_DIM=768
GEMINI_TASK_TYPE=classification
GEMINI_EMB_RATE_LIMIT=2         # lower QPS for dev; 0 disables
GEMINI_EMB_MAX_CALLS=200        # cap per run; 0 disables
GEMINI_EMB_DRYRUN=0             # must be 0 (no mocks in dev)

# ========= Optional Local Transformer (disabled by default) =========
# Enable only if needed for offline tests; adjust DIM to model spec.
SENTENCE_TFM_MODEL=
SENTENCE_TFM_DIM=
SENTENCE_TFM_DRYRUN=0

# ========= Crawl =========
HTTP_USER_AGENT=Mozilla/5.0 (compatible; IABVerticalizerBot/1.0; +https://internal.example)
HTTP_TIMEOUT=25

# ========= Database (Postgres) =========
DB_DSN=postgresql+psycopg2://user:pass@localhost:5432/verticalizer

# ========= S3 / MinIO (optional) =========
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=verticalizer-dev
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_REGION=us-east-1

# ========= Logging & Misc =========
LOG_LEVEL=INFO
PYTHONUNBUFFERED=1
EMB_CACHE_DIR=.embcache
